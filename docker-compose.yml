services:
  waha:
    image: devlikeapro/waha
    container_name: choresir-waha
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      WAHA_LOG_LEVEL: info
      WAHA_PRINT_QR: "True"
    volumes:
      - waha-data:/app/sessions

volumes:
  waha-data:
    driver: local
